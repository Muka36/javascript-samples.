var isloggedin = true;
var emailverified = false;
var cardpurchase = true;

// 
 
if (isloggedin || emailverified || cardpurchase) {
    console.log("Your are ready get course");
} else {
    console.log("Something is missing");
}