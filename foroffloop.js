const names = ['netflix', 'amazon', 'flipkart', 'hotstar', 'snapdeal', 'zee'];

// for of loop for array only

for (const i of names) {
    console.log(i);
}

const sum = {
   yt : "youtube",
   nt: 'netflix',
   bt: 'button',
   mt: 'bike',
};

// for in loop for objects only

for (const n in sum) {
    console.log (`key is: ${n} and value is: ${sum[n]}`);
}

